// how would u check number is integer or not


// JavaScript provides the built-in method Number.isInteger() to check whether a value is an integer. It returns true if the value is an integer, otherwise false.

console.log(Number.isInteger(10));
console.log(Number.isInteger(10.5));
console.log(Number.isInteger(-5));
console.log(Number.isInteger(0));